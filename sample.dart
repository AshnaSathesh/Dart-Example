class tech{

  var person, time;
  tech=(this.person,this.time);
  var get person=> mentor || learner;
  set person()=> time=mentor &&( time ?? 0);
  var get stack=> ui ||webdevlp||go||python||flutter
  set stack(var value)=> noofmentors=value && time  
}

void main(){
  var tech=('mentor',2);
   